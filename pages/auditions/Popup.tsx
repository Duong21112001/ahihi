import { DialogContent, DialogTitle } from "@/components/ui/dialog";
import React from "react";

const Popup = () => {
  return (
    <DialogContent>
      <DialogTitle>Chúc mừng bạn đã hoàn thành bài thi thử</DialogTitle>
    </DialogContent>
  );
};

export default Popup;
